{
  "name": "tabris_redux_example",
  "version": "0.1.0",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "npm run build && npm run lint",
    "lint": "tslint --project . 'src/**/*.ts' 'src/**/*.tsx'",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "start": "npm-run-all --parallel watch serve",
    "serve": "tabris serve"
  },
  "dependencies": {
    "@types/node": "^10.12.2",
    "redux": "^4.0.1",
    "tabris": "^2.6.2",
    "tabris-decorators": "^2.6.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "tslint": "^5.11.0",
    "typescript": "^3.1.6"
  }
}
